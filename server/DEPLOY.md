# 🚀 Деплой Metro Boom на Ubuntu сервер

## Информация о сервере

- **ОС**: Ubuntu 24.04 LTS
- **IP**: 89.104.67.245
- **URL сайта**: http://89.104.67.245

## ⚡ Быстрый деплой

### Шаг 1: Скопируйте проект на сервер

```bash
# С вашего Mac
scp -r . username@89.104.67.245:/opt/metro-boom/
```

Или через Git:

```bash
# На сервере
git clone ВАШ_РЕПОЗИТОРИЙ /opt/metro-boom
```

### Шаг 2: Подключитесь к серверу

```bash
ssh username@89.104.67.245
```

### Шаг 3: Первоначальная настройка (один раз)

```bash
cd /opt/metro-boom
chmod +x server/*.sh
./server/setup-server.sh
```

### Шаг 4: Запустите деплой

```bash
./server/deploy-ubuntu.sh
```

### Шаг 5: Готово!

Откройте в браузере: **http://89.104.67.245**

## 📋 Управление сервером

### Проверить статус

```bash
docker-compose ps
```

### Посмотреть логи

```bash
docker-compose logs -f
```

### Перезапустить сайт

```bash
docker-compose restart
```

### Остановить сайт

```bash
docker-compose down
```

### Обновить сайт

```bash
git pull                    # Если используете Git
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 🔧 Настройка домена (опционально)

Если у вас есть домен, настройте DNS A-запись:

```
Тип: A
Имя: @ (или ваш поддомен)
Значение: 89.104.67.245
```

Затем обновите nginx.conf:

```nginx
server_name your-domain.com www.your-domain.com;
```

## 🔒 Настройка HTTPS (опционально)

### Установка Certbot

```bash
sudo apt-get install -y certbot python3-certbot-nginx
```

### Получение сертификата

```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

### Автообновление сертификата

```bash
sudo certbot renew --dry-run
```

## 🔥 Файрвол

Порты, которые должны быть открыты:

- **22** - SSH
- **80** - HTTP
- **443** - HTTPS (если настроен)

```bash
# Проверить статус
sudo ufw status

# Открыть порт
sudo ufw allow 80/tcp
```

## 📊 Мониторинг

### Использование ресурсов

```bash
docker stats
```

### Место на диске

```bash
df -h
```

### Логи системы

```bash
journalctl -u docker -f
```

## 🆘 Решение проблем

### Сайт не открывается

1. Проверьте, запущен ли контейнер:
```bash
docker-compose ps
```

2. Проверьте логи:
```bash
docker-compose logs
```

3. Проверьте файрвол:
```bash
sudo ufw status
```

### Ошибка при сборке

```bash
# Очистите Docker кэш
docker system prune -a

# Пересоберите
docker-compose build --no-cache
```

### Нет места на диске

```bash
# Очистите неиспользуемые образы
docker image prune -a

# Очистите логи
sudo journalctl --vacuum-time=7d
```

## 📁 Структура на сервере

```
/opt/metro-boom/
├── src/                    # Исходный код
├── public/                 # Статические файлы
├── server/                 # Скрипты деплоя
├── Dockerfile              # Docker конфигурация
├── docker-compose.yml      # Docker Compose
├── nginx.conf              # Nginx конфигурация
└── package.json            # Зависимости
```

## ✅ Чеклист деплоя

- [ ] Сервер настроен (setup-server.sh)
- [ ] Проект скопирован на сервер
- [ ] Docker установлен и работает
- [ ] Контейнер запущен (docker-compose up -d)
- [ ] Сайт открывается по IP
- [ ] Файрвол настроен
- [ ] (Опционально) Домен настроен
- [ ] (Опционально) HTTPS настроен

## 🎯 Готово!

Ваш сайт Metro Boom доступен по адресу: **http://89.104.67.245**

Делитесь ссылкой! 🚀
