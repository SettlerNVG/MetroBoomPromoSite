# 🔧 Настройка Metro Boom для публичного доступа

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Настройка ngrok

### 2.1 Регистрация

1. Перейдите на: https://dashboard.ngrok.com/signup
2. Зарегистрируйтесь (бесплатно)

### 2.2 Получение токена

1. Войдите в аккаунт
2. Перейдите: https://dashboard.ngrok.com/get-started/your-authtoken
3. Скопируйте ваш authtoken

### 2.3 Установка токена

```bash
ngrok config add-authtoken ВАШ_ТОКЕН
```

Пример:
```bash
ngrok config add-authtoken 2abc123def456ghi789jkl
```

## Шаг 3: Запуск

```bash
./start-public.sh
```

Или для фонового режима:

```bash
./scripts/deploy-public.sh
```

## ✅ Готово!

Вы получите публичную ссылку типа: `https://xxxx-xx-xxx-xxx-xxx.ngrok-free.app`

Эта ссылка:
- ✅ Доступна из любой точки мира
- ✅ Работает на любых устройствах
- ✅ Не требует настройки роутера/firewall
- ✅ Бесплатна (с ограничениями ngrok free tier)

## 📋 Управление

```bash
# Проверить статус и получить ссылку
./scripts/status.sh

# Остановить сервисы
./scripts/stop-public.sh
```

## 🔄 Работа с закрытой крышкой Mac

Хотите закрыть крышку Mac, но чтобы сайт продолжал работать?

```bash
# Запретить сон при закрытой крышке
./scripts/prevent-sleep.sh

# Запустить сайт
./scripts/deploy-public.sh

# Закройте крышку - сайт продолжит работать!
```

⚠️ **Важно**: Mac должен быть подключен к зарядке!

Подробная инструкция: [ALWAYS_ON.md](ALWAYS_ON.md)

## 📱 Использование

После запуска:

1. Откройте `PUBLIC_URL.txt` - там ваша ссылка
2. Или выполните `./scripts/status.sh`
3. Делитесь ссылкой с кем угодно!

## ⚠️ Ограничения бесплатного ngrok

- Ссылка меняется при каждом перезапуске
- Ограничение на количество подключений
- Предупреждение при первом открытии

Для постоянной ссылки - купите ngrok Pro или используйте VPS сервер.

## 🆘 Проблемы?

### Ошибка "authentication failed"

```bash
# Проверьте токен
ngrok config check

# Переустановите токен
ngrok config add-authtoken ВАШ_ТОКЕН
```

### Порт 3000 занят

Измените порт в `vite.config.js`:

```javascript
server: {
  port: 8080,
  host: true
}
```

И в скриптах замените `3000` на `8080`.

### Сайт не запускается

```bash
# Проверьте логи
tail -f logs/vite.log
tail -f logs/ngrok.log

# Остановите все процессы
./scripts/stop-public.sh

# Запустите заново
./scripts/deploy-public.sh
```

## 🎯 Готово!

Теперь ваш сайт доступен из любой точки мира! 🌍
