# 🔋 Постоянная работа сайта (Mac с закрытой крышкой)

## Быстрая настройка

### Шаг 1: Запретить сон при закрытой крышке

```bash
./scripts/prevent-sleep.sh
```

Введите пароль администратора когда попросит.

### Шаг 2: Запустить сайт

```bash
./scripts/deploy-public.sh
```

### Шаг 3: Закрыть крышку Mac

Теперь можно закрыть крышку - сайт продолжит работать! 🎉

## ⚠️ Важные условия

Для работы с закрытой крышкой Mac должен:

1. ✅ **Быть подключен к зарядке** (обязательно!)
2. ✅ Иметь подключение к WiFi/Ethernet
3. ✅ Не быть в режиме энергосбережения

## 🔄 Восстановление обычного режима

Когда сайт больше не нужен:

```bash
# Остановить сайт
./scripts/stop-public.sh

# Восстановить обычный режим сна
./scripts/restore-sleep.sh
```

## 🎯 Полная инструкция "запустил и забыл"

### 1. Первоначальная настройка (один раз)

```bash
# Настроить ngrok (если еще не сделали)
ngrok config add-authtoken ВАШ_ТОКЕН

# Запретить сон
./scripts/prevent-sleep.sh
```

### 2. Запуск сайта

```bash
./scripts/deploy-public.sh
```

Получите публичную ссылку из `PUBLIC_URL.txt`

### 3. Закрыть крышку

Просто закройте крышку Mac - всё продолжит работать!

### 4. Проверка (с другого устройства)

Откройте ссылку из `PUBLIC_URL.txt` на телефоне/планшете - сайт работает! ✅

## 🔧 Автозапуск при включении Mac

Чтобы сайт запускался автоматически:

### Через Automator

1. Откройте **Automator**
2. Создайте **Приложение**
3. Добавьте **Запустить команду Shell**
4. Вставьте:

```bash
cd /Users/nikitooooooozy/Documents/GitHub/MetroBoomPromoSite
./scripts/deploy-public.sh
sleep 15
osascript -e 'tell application "Terminal" to close first window' & exit
```

5. Сохраните как **"Metro Boom Server.app"**
6. **Системные настройки** → **Основные** → **Объекты входа** → добавьте приложение

Теперь при каждом включении Mac:
- ✅ Сайт запустится автоматически
- ✅ Можно закрыть крышку
- ✅ Сайт будет работать постоянно

## 📱 Как использовать

1. Включите Mac
2. Дождитесь запуска (15-30 секунд)
3. Откройте `PUBLIC_URL.txt` или выполните `./scripts/status.sh`
4. Скопируйте ссылку
5. Закройте крышку Mac
6. Делитесь ссылкой с кем угодно!

## 💡 Советы

### Для максимальной стабильности:

- Подключите Mac к Ethernet (стабильнее WiFi)
- Используйте качественное зарядное устройство
- Поставьте Mac в прохладное место
- Периодически проверяйте статус: `./scripts/status.sh`

### Мониторинг:

```bash
# Проверить статус
./scripts/status.sh

# Посмотреть логи
tail -f logs/vite.log
tail -f logs/ngrok.log

# Проверить, что Mac не спит
pmset -g
```

## 🆘 Проблемы

### Mac всё равно засыпает

```bash
# Проверьте настройки
pmset -g

# Убедитесь, что Mac подключен к питанию
# Повторите настройку
./scripts/prevent-sleep.sh
```

### Сайт перестал работать после закрытия крышки

```bash
# Проверьте WiFi - он должен оставаться подключенным
# В настройках WiFi отключите "Автоматическое отключение при сне"
```

### Ссылка ngrok перестала работать

```bash
# Ngrok бесплатный меняет ссылку при перезапуске
# Проверьте новую ссылку:
cat PUBLIC_URL.txt

# Или перезапустите:
./scripts/stop-public.sh
./scripts/deploy-public.sh
```

## 🔋 Энергопотребление

При закрытой крышке Mac потребляет:
- ~10-15W (минимальный режим)
- Безопасно для длительной работы
- Рекомендуется хорошая вентиляция

## ✅ Итоговый чеклист

- [ ] Установлен ngrok и настроен authtoken
- [ ] Выполнен `./scripts/prevent-sleep.sh`
- [ ] Запущен `./scripts/deploy-public.sh`
- [ ] Mac подключен к зарядке
- [ ] Mac подключен к WiFi/Ethernet
- [ ] Получена публичная ссылка из `PUBLIC_URL.txt`
- [ ] Ссылка работает с других устройств
- [ ] Крышка закрыта, сайт работает! 🎉

## 🎮 Готово!

Теперь ваш Mac работает как мини-сервер:
- ✅ Сайт доступен 24/7
- ✅ Можно закрыть крышку
- ✅ Публичная ссылка работает из любой точки мира
- ✅ Автозапуск при включении (если настроили)

Делитесь ссылкой и наслаждайтесь! 🚀
