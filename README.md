# Metro Boom - Официальный сайт

Современный React-сайт для кооперативной приключенческой игры Metro Boom, созданной на Unreal Engine 5.

## 🎮 О игре

Metro Boom - это захватывающая история о двух совершенно разных персонажах, которые случайно встречаются, скрываясь от полиции, и отправляются исследовать таинственный подземный мир метро.

### Персонажи

**Девочка-дьявол** 😈
- Может лазить по стенам
- Видит сквозь стены
- Обожает колу
- Обладает сверхъестественными способностями

**Парень-художник** 🎨
- Любит рисовать граффити
- Использует свои навыки для решения головоломок
- Быстрый и ловкий

### Геймплей

- Кооперативное прохождение на двоих
- Взаимодействие с предметами окружения
- Уникальные способности каждого персонажа
- Стелс-механики и тактическое прохождение
- Захватывающий сюжет первой миссии

### Противники

**Полицейские** 👮
- Опасны для обоих персонажей
- Сразу замечают и преследуют
- Требуют скрытности и командной работы

**Демоны** 👹
- Опасны только для парня
- Девочка-дьявол невидима для них
- Парень может незаметно оглушить демона

## 🚀 Технологии

- **React 18** - UI библиотека
- **Vite** - Быстрый сборщик
- **Ngrok** - Публичный доступ
- **Docker** - Контейнеризация (опционально)

## 📦 Установка и запуск

### 🌐 Публичный деплой (рекомендуется)

Создает публичную ссылку, доступную из любой точки мира:

```bash
# Быстрый запуск
./start-public.sh

# Или полный деплой с логами
./scripts/deploy-public.sh
```

После запуска получите публичную ссылку типа: `https://xxxx.ngrok.app`

### 💻 Локальная разработка

```bash
# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev
```

Сайт будет доступен: http://localhost:3000

### 🛑 Управление

```bash
# Проверить статус
./scripts/status.sh

# Остановить сервисы
./scripts/stop-public.sh
```

### 💤 Работа с закрытой крышкой

Хотите закрыть Mac, но чтобы сайт продолжал работать?

```bash
# Запретить сон при закрытой крышке
./scripts/prevent-sleep.sh

# Mac должен быть подключен к зарядке!
```

Подробнее: [ALWAYS_ON.md](ALWAYS_ON.md)

## 🐳 Деплой на Ubuntu сервер

Сайт настроен для деплоя на Ubuntu 24.04 LTS сервер (IP: 89.104.67.245)

### Быстрый деплой с Mac

```bash
# Деплой одной командой
./deploy-to-server.sh root
```

### Ручной деплой

```bash
# 1. Скопируйте проект на сервер
scp -r . root@89.104.67.245:/opt/metro-boom/

# 2. Подключитесь к серверу
ssh root@89.104.67.245

# 3. Запустите деплой
cd /opt/metro-boom
./server/deploy-ubuntu.sh
```

### Управление на сервере

```bash
docker-compose ps        # Статус
docker-compose logs -f   # Логи
docker-compose restart   # Перезапуск
docker-compose down      # Остановка
```

Подробная инструкция: [server/DEPLOY.md](server/DEPLOY.md)

## 📁 Структура проекта

```
metro-boom-site/
├── src/                   # Исходный код
│   ├── components/        # React компоненты
│   ├── App.jsx
│   └── main.jsx
├── public/                # Статические файлы
│   ├── images/            # Изображения
│   ├── videos/            # Видео
│   └── downloads/         # Файлы для скачивания
├── server/                # Скрипты для Ubuntu сервера
│   ├── deploy-ubuntu.sh   # Деплой на сервере
│   ├── setup-server.sh    # Настройка сервера
│   └── DEPLOY.md          # Инструкция по деплою
├── deploy-to-server.sh    # Деплой с Mac на сервер
├── Dockerfile             # Docker конфигурация
├── docker-compose.yml     # Docker Compose
├── nginx.conf             # Nginx конфигурация
└── QUICKSTART.md          # Быстрый старт
```

## 🖼️ Добавление изображений

### Фотографии команды
Поместите в папку `public/images/`:

- `denis.jpg` - Кочетов Денис (рекомендуемый размер: 400x400px)
- `vladislav.jpg` - Иманаков Владислав (рекомендуемый размер: 400x400px)
- `vladimir.jpg` - Антонов Владимир (рекомендуемый размер: 400x400px)
- `nikita.jpg` - Догадин Никита (рекомендуемый размер: 400x400px)

### Фоновое изображение
Поместите в папку `public/images/backgrounds/`:

- `hero-bg.jpg` - Фон главной секции (рекомендуемый размер: 1920x1080px)
  - Используйте скриншот из игры Metro Boom
  - Формат: JPG или PNG
  - Оптимизируйте размер файла (до 500 KB)

## 📹 Добавление видео

Поместите видеофайлы в папку `public/videos/`:

- `trailer.mp4` - Геймплейный трейлер
- `trailer-thumb.jpg` - Превью трейлера (1280x720px)
- `devlog.mp4` - Процесс разработки
- `devlog-thumb.jpg` - Превью процесса (1280x720px)

**Рекомендации для видео:**
- Формат: MP4 (H.264)
- Разрешение: 1920x1080 или 1280x720
- Размер: до 50-100 MB на видео

## 📦 Добавление файлов для скачивания

Поместите архивы в папку `public/downloads/`:

- `metro-boom-build.zip` - Готовая сборка игры для Windows
- `metro-boom-source.zip` - Исходные файлы проекта UE5

**Рекомендации для архивов:**
- Формат: ZIP (лучшая совместимость) или RAR
- Билд: до 500 MB
- Исходники: до 2 GB
- Исключите из исходников: Binaries, Intermediate, Saved папки

## 🌐 Деплой на удаленный сервер

### Через SSH

```bash
# Скопируйте проект на сервер
scp -r . user@your-server:/path/to/metro-boom-site

# Подключитесь к серверу
ssh user@your-server

# Перейдите в директорию проекта
cd /path/to/metro-boom-site

# Запустите деплой
./deploy.sh
```

### Через Git

```bash
# На сервере клонируйте репозиторий
git clone your-repo-url
cd metro-boom-site

# Запустите деплой
./deploy.sh
```

## 🔧 Настройка порта

По умолчанию сайт запускается на порту 80. Чтобы изменить порт, отредактируйте `docker-compose.yml`:

```yaml
ports:
  - "8080:80"  # Изменить первое число на нужный порт
```

## 📊 Мониторинг

```bash
# Проверить статус контейнера
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Посмотреть использование ресурсов
docker stats
```

## 🔄 Обновление сайта

```bash
# Остановите контейнер
docker-compose down

# Обновите код (через git или вручную)
git pull

# Пересоберите и запустите
./deploy.sh
```

## 🛠️ Разработка

### Команды npm

- `npm run dev` - Запуск dev-сервера
- `npm run build` - Сборка для продакшена
- `npm run preview` - Предпросмотр production build
- `npm run lint` - Проверка кода

### Горячая перезагрузка

При разработке все изменения применяются автоматически благодаря Vite HMR.

## 📝 Лицензия

© 2024 Metro Boom Team. Все права защищены.

## 🤝 Команда

- **Кочетов Денис** - PM и геймдизайнер
- **Иманаков Владислав** - PM и программист
- **Антонов Владимир** - Сценарист
- **Догадин Никита** - DevOps

---

Сделано с ❤️ на Unreal Engine 5
